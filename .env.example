# Application Configuration
NODE_ENV=production
PORT=3000

# Database Configuration (Override with actual production values)
DB_HOST=mysql-server
DB_PORT=3306
DB_USERNAME=qykcart_user
DB_PASSWORD=secure-production-password
DB_DATABASE=qykcart_production

# JWT Configuration (Use a secure secret in production)
JWT_SECRET=secure-production-jwt-secret-key
JWT_EXPIRES_IN=24h

# API Configuration
API_PREFIX=api/v1
SWAGGER_PATH=api/docs